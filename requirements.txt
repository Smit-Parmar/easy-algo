# === Core Dependencies ===
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0
loguru>=0.7.2
rich>=13.5.2

# === Visualization (for future phases) ===
matplotlib>=3.8.0
plotly>=5.17.0

# === Technical Indicators ===
ta>=0.10.2
# (You can optionally use TA-Lib if installed locally)
# TA-Lib==0.4.28

# === Backtesting Frameworks (Phase 3+ Ready) ===
backtesting>=0.3.3
vectorbt>=0.26.1

# === Async & WebSocket (Phase 4+ for live trading) ===
websockets>=12.0
aiohttp>=3.9.0

# === Optional Web Interface / REST API ===
fastapi>=0.111.0
uvicorn>=0.30.0

# === Data Storage & Formats ===
pyarrow>=15.0.0
fastparquet>=2024.0.0